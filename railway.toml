[build]
dockerfilePath = "Dockerfile"
dockerBuildTarget = "api"

[deploy]
startCommand = "sh -c 'echo Starting API... && npx prisma migrate deploy && echo Migrations done, starting server... && node dist/src/server.js'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
